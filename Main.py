#Guessing game,Player gets a card and needs to guess if his cards value is higher or lower than the next one

from tkinter import *
import Front

if True:


    root = Tk()
    gui = Front.GUI(root).pack()
    root.mainloop()